# 🌃 2087 — AI가 지배하는 도시에서 당신은 누구인가?

사이버펑크 세계관 성격 테스트. 7개의 질문으로 2087년 AI 도시 속 당신의 정체를 밝혀냅니다.

## 결과 유형 (6종)

- **⟨/⟩ CODE:LEVEL** — AI를 창조하는 천재 해커
- **◈ GHOST:RUNNER** — 그림자 속의 정보 브로커
- **◎ SYN:APSE** — AI와 뇌가 융합된 하이브리드
- **△ ANALOG** — 구시대의 반항아
- **◬ ORACLE** — AI를 신으로 섬기는 사제
- **◇ MIRROR** — 자아를 잃은 존재

## 로컬 실행

```bash
npm install
npm run dev
```

## 배포 (Vercel)

### 방법 1: Vercel CLI

```bash
npm install -g vercel
vercel
```

### 방법 2: GitHub 연동

1. 이 프로젝트를 GitHub에 push:
   ```bash
   git init
   git add .
   git commit -m "initial commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/2087-identity-test.git
   git push -u origin main
   ```

2. [vercel.com](https://vercel.com) 접속 → "Add New Project"
3. GitHub 레포 선택
4. Framework Preset: **Vite** 자동 감지
5. **Deploy** 클릭 → 끝!

배포 후 `https://your-project.vercel.app` 주소를 받게 됩니다.

## 커스텀 도메인 (선택)

Vercel 대시보드 → Settings → Domains에서 커스텀 도메인 연결 가능 (무료).

## OG 이미지

`public/og-image.png`에 1200x630px 이미지를 넣으면 SNS 공유 시 미리보기로 표시됩니다.

## 기술 스택

- React 18
- Vite 5
- 순수 CSS 애니메이션 (외부 라이브러리 없음)
- Google Fonts (Orbitron, Noto Sans KR, Share Tech Mono)

## 라이선스

MIT
